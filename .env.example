# Copie para .env e preencha. NÃO comite o .env (só o .env.example).
# No Portainer, cadastre estas como variáveis de ambiente do stack.

# --- Banco ---
DB_ROOT_PASS=troque-uma-senha-forte-root
DB_PASS=troque-uma-senha-forte-mbillingUser

# --- Rede / VoIP ---
PUBLIC_IP=203.0.113.10          # IP PÚBLICO do nó MB (usado no SDP/PJSIP)
RTP_START=10000
RTP_END=20000

# --- AMI (mantenha o padrão do MagnusBilling salvo se souber o que faz) ---
AMI_SECRET=magnussolution
